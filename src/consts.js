const DNA_PORT = 8800
const CONDUCTOR_HTTP_PORT = 8080
const HHA_INSTANCE_ID = "holo-hosting-app"
const HS_INSTANCE_ID = "happ-store"

module.exports = {DNA_PORT, CONDUCTOR_HTTP_PORT, HHA_INSTANCE_ID, HS_INSTANCE_ID}
